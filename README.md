# Next Level Thought

A personal JavaScript learning journal - documenting daily concepts, code snippets, and practice problems from fundamentals to advanced topics.

## About

This repository is a structured, day-by-day log of JavaScript concepts explored and practiced over time. Each file contains explanations, examples, and hands-on code for a specific topic, making it a handy reference for revisiting concepts.

## Project Structure

```
Next_level_thought/
├── March 2026/
├── November 2023/
├── October 2023/
├── September 2023/
├── .gitignore
├── Dos.txt
├── package.json
└── README.md
```

## Topics Covered

### September 2023

| Date    | Topic              |
| ------- | ------------------ |
| Sept 27 | Types of functions |
| Sept 28 | Git and Github     |

### October 2023

| Date   | Topic                                                                                 |
| ------ | ------------------------------------------------------------------------------------- |
| Oct 01 | Object create                                                                         |
| Oct 02 | optional chaining operator (`?.`)                                                     |
| Oct 03 | Permutations and combinations formulas                                                |
| Oct 04 | Password Length check                                                                 |
| Oct 05 | Nullish Coalescing Operator (`??`)                                                    |
| Oct 06 | `slice()` and `splice()`                                                              |
| Oct 07 | `push()`, `pop()`, `shift()`, `unshift()`                                             |
| Oct 08 | `concat()`, `join()`, `split()`, `toString()`                                         |
| Oct 09 | `slice()`, `substring()`, `substr()`                                                  |
| Oct 10 | `isNaN()`                                                                             |
| Oct 11 | `length` Property                                                                     |
| Oct 12 | `Set`, `Array.from()`                                                                 |
| Oct 13 | JavaScript Object, `Object.assign()`, `Object.keys()`, `Object.values()`              |
| Oct 14 | Date Functions                                                                        |
| Oct 15 | `Array.isArray()`                                                                     |
| Oct 16 | `Math.round()`, `Math.ceil()`, `Math.floor()`                                         |
| Oct 17 | Atlassian Tools — Jira, Bitbucket, Confluence                                         |
| Oct 18 | `Math.trunc()`, `Math.pow()`, `Math.sqrt()`, `Math.abs()`, `Math.max()`, `Math.min()` |
| Oct 19 | `filter()`                                                                            |
| Oct 20 | `reduce()`                                                                            |
| Oct 21 | `find()`                                                                              |
| Oct 22 | `findIndex()` and `includes()`                                                        |
| Oct 23 | `some()` and `every()`                                                                |
| Oct 24 | `sort()`                                                                              |
| Oct 25 | `replace()` and `replaceAll()`                                                        |
| Oct 26 | `trim()`, `trimStart()`, `trimEnd()`                                                  |
| Oct 27 | Difference between `==` vs `===`                                                      |
| Oct 28 | `indexOf()` and `lastIndexOf()`                                                       |
| Oct 29 | `charAt()` and `charCodeAt()`                                                         |
| Oct 30 | How to Print Nested Objects                                                           |
| Oct 31 | `padStart()` and `padEnd()`                                                           |

### November 2023

| Date   | Topic                           |
| ------ | ------------------------------- |
| Nov 06 | `startsWith()` and `endsWith()` |
| Nov 07 | `reverse()` and `flat()`        |
| Nov 08 | `Array.map()`                   |
| Nov 09 | Practice Question               |
| Nov 10 | Practice Question               |
| Nov 11 | Practice Question               |
| Nov 12 | Practice Question               |
| Nov 13 | CSV Read                        |
| Nov 14 | How to Break a `forEach` Loop   |

### March 2026

| Date   | Topic                                                  |
| ------ | ------------------------------------------------------ |
| Mar 10 | nvm Usage                                              |
| Mar 11 | `Object.freeze()` and `Object.seal()`                  |
| Mar 12 | JavaScript Console Methods                             |
| Mar 12 | JavaScript Quirks (array `+` operator, comma operator) |
| Mar 22 | JavaScript Error Types                                 |
| Mar 24 | Graceful Shutdown                                      |
| Mar 25 | Types and Interfaces in Typescript                     |
| Mar 26 | Currying in Javascript                                 |

## Key Concepts at a Glance

- **Fundamentals** — Data types, operators (`?.`, `??`, `==` vs `===`), type checking (`isNaN`, `Array.isArray`), JS quirks
- **Strings** — `slice`, `substring`, `replace`, `trim`, `padStart/End`, `charAt`, `startsWith/endsWith`
- **Arrays** — `map`, `filter`, `reduce`, `find`, `sort`, `some/every`, `flat`, `reverse`, `splice`
- **Objects** — `Object.create`, `Object.assign`, `Object.keys/values`, `Object.freeze/seal`, nested objects
- **Functions** — Currying, partial application, generic curry helper
- **Math** — `round`, `ceil`, `floor`, `trunc`, `pow`, `sqrt`, `abs`, `max`, `min`
- **Dates** — JavaScript Date functions
- **Node.js** — Express.js v5, nvm, CSV reading, graceful shutdown
- **Error Handling** — JavaScript error types
- **Tooling** — Git, GitHub, Jira, Bitbucket, Confluence
- **Typescript** — Types and Interfaces

## Getting Started

```bash
# Clone the repository
git clone https://github.com/ChiragParmar2207/Next_level_thought.git

# Navigate into the project
cd Next_level_thought

# Install dependencies
npm install

# Run any topic file
node "October 2023/oct06_C.js"
```
